class MessageReplayModel {
  final String? message;
  final String? username;
  final String? messageType;
  final bool? isMe;

  MessageReplayModel({this.message, this.username, this.messageType, this.isMe, });
}